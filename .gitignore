# .gitignore

# Service account keys - CRITICAL
google_credentials.json
*.json # Be careful with this wildcard if you have other JSON files you WANT to track. 
       # If so, be more specific, e.g., service_account_*.json or just google_credentials.json

# Data files (especially if large or sensitive, like your original CSV if not meant to be public)
*.csv 
*.xlsx

# Python specific
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg
*.log

# Virtual environment folder (if you create one in your project directory)
venv/
.venv/
env/
.env # For environment variable files which often contain secrets

# IDE / OS specific files
.DS_Store
.vscode/
.idea/
*.swp

# Added by Claude Task Master
# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific
# Task files
tasks.json
tasks/ 